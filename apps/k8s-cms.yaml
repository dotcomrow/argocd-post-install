# -----------------------------------------------------------------------------
# Argo CD: Git repo secret (unchanged)
# -----------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: directus
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  url: git@github.com:dotcomrow/k8s-cms.git
  sshPrivateKey: |
      -----BEGIN OPENSSH PRIVATE KEY-----
      
      -----END OPENSSH PRIVATE KEY-----
  insecure: "true"
---
# -----------------------------------------------------------------------------
# Argo CD: Project (unchanged)
# -----------------------------------------------------------------------------
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: directus
  namespace: argocd
spec:
  description: Project for directus app
  sourceRepos:
    - '*'
  destinations:
    - namespace: directus
      server: https://kubernetes.default.svc
    - namespace: vault
      server: https://kubernetes.default.svc
    - namespace: yugabyte
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
---
# -----------------------------------------------------------------------------
# Argo CD: Your existing "kafka" application (unchanged)
# -----------------------------------------------------------------------------
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: directus
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: directus
  source:
    repoURL: git@github.com:dotcomrow/k8s-cms.git
    targetRevision: prod
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: directus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m