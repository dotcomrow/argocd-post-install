# -----------------------------------------------------------------------------
# Argo CD: Git repo secret (unchanged)
# -----------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: kafka
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  url: git@github.com:dotcomrow/k8s-kafka.git
  sshPrivateKey: |
    -----BEGIN OPENSSH PRIVATE KEY-----
    
    -----END OPENSSH PRIVATE KEY-----
  insecure: "true"
---
# -----------------------------------------------------------------------------
# Argo CD: Project (unchanged)
# -----------------------------------------------------------------------------
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: kafka
  namespace: argocd
spec:
  description: Project for kafka app
  sourceRepos:
    - '*'
  destinations:
    - namespace: kafka
      server: https://kubernetes.default.svc
    - namespace: vault
      server: https://kubernetes.default.svc
    - namespace: yugabyte
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
---
# -----------------------------------------------------------------------------
# Argo CD: Your existing "kafka" application (unchanged)
# -----------------------------------------------------------------------------
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka
  namespace: argocd
spec:
  project: kafka
  source:
    repoURL: git@github.com:dotcomrow/k8s-kafka.git
    targetRevision: prod
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: kafka
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
---
# -----------------------------------------------------------------------------
# NEW: Argo CD Application to deploy Debezium Platform (Helm) to namespace "kafka"
# Notes:
# - Pulls directly from upstream Debezium Platform repo (public).
# - Uses the chart in /helm with values to point at your Connect and set CORS.
# - fullnameOverride keeps names predictable (svc/debezium-platform-ui, etc.).
# - Keep this separate from your existing standalone Debezium UI so you can A/B.
# -----------------------------------------------------------------------------
# --- Debezium Platform UI via Helm (Argo CD) — FIXED values ---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: debezium-platform
  namespace: argocd
spec:
  project: kafka
  destination:
    server: https://kubernetes.default.svc
    namespace: kafka
  source:
    repoURL: https://github.com/debezium/debezium-platform.git
    targetRevision: main
    path: helm
    helm:
      values: |
        fullnameOverride: debezium-platform

        # We run our own Kafka stack
        kafka:
          enabled: false
        connect:
          enabled: false
        registry:
          enabled: false

        # Use bundled Postgres for the Conductor backend (no PV)
        database:
          enabled: true
          image:
            registry: docker.io
            repository: bitnami/postgresql
            tag: 16
          auth:
            username: debezium
            password: debezium
            database: conductor
          primary:
            persistence:
              enabled: false

        # Debezium UI settings (no ingress; we’ll keep using oauth2-proxy)
        ui:
          fullnameOverride: debezium-ui
          env:
            KAFKA_CONNECT_URIS: "http://kafka-connect.kafka.svc.cluster.local:8083"
            QUARKUS_HTTP_PROXY_PROXY_ADDRESS_FORWARDING: "true"
            QUARKUS_HTTP_CORS: "true"
            QUARKUS_HTTP_CORS_ORIGINS: "https://debezium-ui.app.suncoast.systems"
            QUARKUS_LOG_LEVEL: "DEBUG"
          service:
            type: ClusterIP
            port: 8080
          ingress:
            enabled: false

        # Conductor API (no ingress; same reasoning)
        conductor:
          ingress:
            enabled: false

        # Global chart ingress toggle (off)
        ingress:
          enabled: false
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

